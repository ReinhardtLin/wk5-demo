<%= form_for @event, :url => { :controller => 'events', :action => 'update', :id => @event }, :html => { :multipart => true } do |f| %>

  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "Update" %>
<% end %>