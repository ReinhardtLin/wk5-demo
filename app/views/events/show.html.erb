<%= @event.name %>
<%= simple_format(@event.description) %>

<%= link_to image_tag(@event.logo.url(:thumb) ), @event.logo.url %>
<p><%= link_to "Back to Index", :controller => 'events', :action => 'index', class: "btn btn-primary" %></p>
